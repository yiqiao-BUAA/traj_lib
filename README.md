run the following command in `traj_lib` to test the model:

```bash
python -m main --model FPMC --dataset NYC --metrics ReCall1
```
dataset can be download at https://drive.google.com/drive/folders/1scEBkoUxGto0qvo5OHnUFPgb3NihD3BI?usp=drive_link